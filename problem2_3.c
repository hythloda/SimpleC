#include <stdio.h>         /*for printf*/
#include <math.h>       /*fot tan(x)*/

int x; /*will be seen everywhere*/

f(double x)
{


int main()

{
  float a, b, c, z;
  a=tan(x)-3;
  b=1;
  y=a+x;
  z=y-b;
 printf("x= %1.8f 1+x-1= y= %11.8f\n",x,z);
 /*will look like x= 0.000000 y=0.0000000*/
 /*must be in for loop to show more than one line of x */
    }

return(0);
}
